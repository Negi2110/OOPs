Here is a **clean, professional, interview-ready paragraph** explaining lambda functions in C++ from **every angle you need**, written as **comments** so you can paste directly into your code or notes.

---

```cpp
/*
    ============================
    🔹 LAMBDA FUNCTIONS IN C++ 🔹
    ============================

    A lambda function in C++ is an anonymous (unnamed) function that can be 
    defined inline where it is needed. Lambdas were introduced in C++11 to make 
    functional-style programming easier, especially for short operations, 
    callbacks, multithreading, STL algorithms, and event-driven code. 
    They are extremely useful in interviews because they demonstrate your 
    understanding of modern C++ and clean coding practices.

    ----------------------------------
    🔸 BASIC SYNTAX OF A LAMBDA
    ----------------------------------
        [capture_list] (parameters) -> return_type {
            // function body
        };

    • capture_list  → controls what variables from the surrounding scope the lambda can access.
    • parameters    → same as normal functions.
    • return_type   → often omitted because compiler deduces it automatically.

    Example:
        auto fun = [](int x) { return x * 2; };

    ----------------------------------
    🔸 USING LAMBDAS IN THREADS
    ----------------------------------
    Lambdas are commonly used with std::thread because they are lightweight, 
    inline, and don't require defining a separate function or class.

        std::thread t([](int x){
            while (x --> 0)
                cout << x << endl;
        }, 10);

    Here, `auto` is required because lambdas have unique compiler-generated types.

    ----------------------------------
    🔸 CAPTURE LIST (VERY IMPORTANT IN INTERVIEWS)
    ----------------------------------
    The capture list decides how the lambda accesses variables outside itself.

    1. []        → captures nothing.
    2. [x]       → captures x by value (copy).
    3. [&x]      → captures x by reference.
    4. [=]       → captures all outer variables by value.
    5. [&]       → captures all outer variables by reference.
    6. [x, &y]   → mix of value and reference captures.

    Example:
        int a = 5, b = 10;
        auto f = [a, &b]() { /* a is copy, b is reference */ };

    ----------------------------------
    🔸 WHY LAMBDAS ARE IMPORTANT (INTERVIEW POINTS)
    ----------------------------------
    • They allow inline function creation (no separate function needed).
    • Essential for using STL algorithms like std::sort, std::for_each, etc.
    • Reduce boilerplate, improve readability.
    • Allow capturing external variables cleanly.
    • Perfect for one-off operations, threading, callbacks, and event handling.
    • Enable functional programming style in C++.
    • Help avoid unnecessary global/static functions.
    • Their types are unique → can only be stored using `auto` or std::function.

    ----------------------------------
    🔸 COMMON INTERVIEW QUESTIONS
    ----------------------------------
    1. What is a lambda and why do we need it?
       → A lambda is an inline anonymous function used for short, temporary 
         operations where defining a separate function is unnecessary.

    2. Explain the capture list.
       → Controls access to external variables (value, reference, or mixed).

    3. Why do we use `auto` with lambdas?
       → Because lambda types are unnamed and unique.

    4. How do lambdas help in multithreading?
       → They let us define thread logic inline without extra functions.

    5. Difference between `[]`, `[=]`, `[&]` in lambdas?
       → No capture, capture by value, capture by reference.

    6. Can a lambda store state?
       → Yes, when it captures variables by value or uses mutable lambdas.

    ----------------------------------
    🔸 LAMBDA WITH MUTABLE
    ----------------------------------
    Lambdas capture by value are const by default. Use `mutable` to allow modification.

        int x = 10;
        auto f = [x]() mutable { x++; };

    ----------------------------------
    🔸 LAMBDA RETURN TYPE
    ----------------------------------
    You can optionally specify the return type:

        auto f = [](int x) -> double {
            return x * 1.5;
        };

    ----------------------------------
    🔸 SUMMARY (INTERVIEW GOLD)
    ----------------------------------
    Lambda functions provide short, inline, anonymous functions with powerful 
    capture semantics, used heavily in modern multithreading and STL algorithms. 
    They help write clean, readable, and expressive code while avoiding unnecessary 
    function declarations. Being comfortable with lambda syntax and behavior is 
    essential for any modern C++ interview.
*/
```

-/*
    ============================================
    🔸 BASIC SYNTAX OF A LAMBDA — WITH EXAMPLES
    ============================================

        [capture_list] (parameters) -> return_type {
            // function body
        };

    ----------------------------------------------------------------------
    1️⃣ CAPTURE LIST — Controls what variables from outside the lambda 
       can be accessed (and how).
    ----------------------------------------------------------------------

        int a = 5, b = 10;

        // Capturing by value
        auto f1 = [a]() {
            return a * 2;   // OK (copy of 'a')
        };

        // Capturing by reference
        auto f2 = [&b]() {
            b++;            // modifies original 'b'
        };

        // Capturing everything by value
        auto f3 = [=]() {
            return a + b;   // both copied
        };

        // Capturing everything by reference
        auto f4 = [&]() {
            a++; b++;       // modifies original values
        };

        // Mixed capture
        auto f5 = [a, &b]() {
            return a + (b * 2);
        };

    ----------------------------------------------------------------------
    2️⃣ PARAMETERS — Works like normal function parameters.
    ----------------------------------------------------------------------

        auto f6 = [](int x, int y) {
            return x + y;
        };

        // Call:
        int sum = f6(3, 4);   // returns 7

    ----------------------------------------------------------------------
    3️⃣ RETURN TYPE — Often omitted, but can be specified when needed.
    ----------------------------------------------------------------------

        // Lambda with explicit return type
        auto f7 = [](int x) -> double {
            return x * 1.5;   // returns double
        };

        double d = f7(4);     // returns 6.0

        // Lambda where return type is auto-deduced by compiler (common)
        auto f8 = [](int x) {
            return x * 2;     // compiler deduces return type = int
        };

    ----------------------------------------------------------------------
    ⭐ FULL EXAMPLE COMBINING ALL THREE:
    ----------------------------------------------------------------------

        int factor = 3;

        auto multiply = [factor] (int x) -> int {
            return x * factor;
        };

        int result = multiply(5);   // returns 5 * 3 = 15

    ----------------------------------------------------------------------
    KEY INTERVIEW POINT:
    ----------------------------------------------------------------------
    Lambdas provide short, inline, anonymous functions with powerful capture 
    semantics. They improve readability, simplify thread creation, and work 
    beautifully with STL algorithms.
*/
